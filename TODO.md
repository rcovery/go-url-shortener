✅ Implement repo integration tests
✅ Implement repo fn
Try to find the coverage of repo tests
Search if it's a good thing to create a "name" type and "NewName" fn (just to parse "name" from request body)
Search if it's a good thing to create a "link" type and "NewLink" fn (same)
Implement service fn
Implement service tests
Implement service fn
